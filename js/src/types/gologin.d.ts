declare module 'gologin' {
  export default class GoLogin {
    constructor({
      autoUpdateBrowser: boolean,
      token: string,
      profile_id: string,
      extra_params: string,
    }) {
      this.job = job
      this.options = options
    }
    start: Function
    stop: Function
  }
}
